**Условие задачи**

Дано бинарное дерево, в каждом узле которого записано **целое** число. Путь в
бинарном дереве — это **последовательность узлов**, в которой каждая пара
соседних узлов соединена ребром. Один и тот же узел **не может** встречаться в
пути **более одного раза**. Путь не обязан проходить через корень дерева.
Требуется найти **максимальную** сумму любого **непустого** пути в данном
бинарном дереве.

**Наблюдения**

Путь может начинаться и заканчиваться в **произвольных узлах** дерева.
Значения узлов могут быть **отрицательными**, поэтому добавление узла в путь
может уменьшить его суммарную сумму. Также путь в дереве не может
разветвляться **более одного раза**.

**Описание алгоритма**

Алгоритм основан на **обходе дерева в глубину (DFS) снизу вверх**. Для каждого
узла рассматриваются два значения: **максимальный путь**, проходящий через
этот узел, и **вклад узла в путь**, который может быть продолжен вверх к
родителю.

**Лемма 1**

Если вклад одного из поддеревьев отрицателен, его невыгодно включать в
путь, так как это уменьшит суммарную сумму. Следовательно, при вычислении
пути через узел учитываются только неотрицательные вклады поддеревьев.

**Доказательство леммы 1.**

Рассмотрим произвольный узел бинарного дерева 𝑣 и путь, проходящий через данный узел. Пусть вклад одного из поддеревьев узла 𝑣 в суммарную величину пути равен числу 
𝑘. Под вкладом понимается максимальная сумма пути, начинающегося в узле 𝑣 и проходящего через соответствующее поддерево.
Если 𝑘 ≥ 0, то включение данного поддерева в путь не уменьшает суммарную сумму пути и, следовательно, может быть выгодным. Если же k<0, то добавление этого поддерева к пути приводит к уменьшению суммарной суммы, так как для любой суммы 
𝑆 справедливо неравенство S+k<S.
Следовательно, путь, не включающий поддерево с отрицательным вкладом, всегда имеет сумму не меньшую, чем путь, в который такое поддерево включено. Из этого следует, что при поиске максимальной суммы пути включение поддеревьев с отрицательным вкладом является нецелесообразным.
Таким образом, при вычислении максимального пути через узел бинарного дерева оптимально учитывать только те поддеревья, вклад которых неотрицателен. Лемма доказана.

**Лемма 2**

Путь, который передаётся родительскому узлу, не может включать оба
поддерева одновременно, так как в этом случае путь разветвляется, что
противоречит определению пути в дереве. Следовательно, вклад вверх может
включать не более одного поддерева.

**Доказательство леммы 2.**
Рассмотрим произвольный узел бинарного дерева 𝑣. По определению путь в дереве представляет собой последовательность узлов, 
в которой каждая пара соседних узлов соединена ребром, и каждый узел может входить в путь не более одного раза.
Следовательно, путь не может иметь разветвлённую структуру.

Предположим противное: путь, передаваемый от узла 𝑣 к его родительскому узлу,
включает одновременно вклад левого и правого поддеревьев узла 𝑣. В этом случае путь
должен проходить из одного поддерева через узел 𝑣 в другое поддерево, а затем продолжаться 
вверх к родительскому узлу. Такая конфигурация приводит к тому, что в узле 𝑣 путь
разветвляется на два направления, что противоречит определению пути как линейной последовательности узлов.
Следовательно, путь, который может быть продолжен вверх к родительскому узлу, может включать не более одного поддерева узла 
𝑣. Из этого следует, что вклад, передаваемый вверх при вычислении максимальной суммы пути, должен учитывать максимум из вкладов левого и правого поддеревьев, но не их сумму.
Таким образом, вклад вверх может включать не более одного поддерева. Лемма доказана.

**Корректность алгоритма**

Каждый узел дерева рассматривается как потенциальный **центр**
максимального пути. Алгоритм перебирает все возможные конфигурации
путей, проходящих через каждый узел, и сохраняет **максимальную** найденную
сумму. Таким образом, среди всех возможных путей в дереве будет найден
путь с максимальной суммой.

**Анализ временной сложности**

Каждый узел дерева посещается **ровно один раз** в ходе обхода DFS.
Следовательно, временная сложность алгоритма составляет O(n), где n —
количество узлов в дереве.

**Анализ затрат памяти**

Алгоритм использует стек рекурсивных вызовов глубиной, **не превышающей
высоту дерева**. В **худшем** случае (вырожденное дерево) затраты памяти
составляют O(n).
